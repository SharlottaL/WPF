<Window x:Class="MenuBar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MenuBar"
        xmlns:userControls="clr-namespace:MenuBar.View.UserControls"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <!--<Rectangle Fill="Red"/>
        <Rectangle Grid.Row="1"  Fill="Green"/>
        <Rectangle Grid.Row="2" Fill="Blue"/>-->
        <!--<Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="30"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <Menu FontSize="18">
                <MenuItem Header="File">
                    <MenuItem Header="New"/>
                    <MenuItem Header="Open"/>
                    <Separator/>
                    <MenuItem Header="Save"/>
                    <MenuItem Header="Save as"/>
                    <Separator/>
                    <MenuItem Header="Quit"/>
                </MenuItem>
                <MenuItem Header="Edit">
                    <MenuItem Header="Find"/>
                    <MenuItem Header="Replace"/>
                    <Separator/>
                    <MenuItem Header="Undo"/>
                    <MenuItem Header="Redo"/>
                </MenuItem>
            </Menu>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <Button  Grid.Column="0" Width="100"  Margin="10"  Content="Кнопка"/>
                <TextBox Grid.Column="1" Width="200"  Margin="10"/>
                
            </Grid>
        </Grid>-->
        <userControls:MenuBar/> 
    </Grid>
</Window>
